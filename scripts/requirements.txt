# Dependencies for scripts/render_resume.py (Stage 8 — format/export).
# One-shot install (creates the gitignored .venv, handles no-pip machines): python scripts/setup.py
python-docx>=1.1
reportlab>=4.0
