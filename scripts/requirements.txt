# Dependencies for scripts/render_resume.py (Stage 8 — format/export).
# Install into a local venv (gitignored): python -m venv .venv && .venv/bin/python -m pip install -r scripts/requirements.txt
# If the system python lacks pip/ensurepip, bootstrap once with the official
# get-pip.py (https://bootstrap.pypa.io/get-pip.py) against the venv interpreter.
python-docx>=1.1
reportlab>=4.0
